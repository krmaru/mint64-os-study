# DEVLOG


## 2026-09-01

### 오늘 한 것

* 운영체제 공부 프로젝트 시작
* Git 설치 및 기본 설정 확인
* 기본 브랜치를 `main`으로 설정
* GitHub SSH 키 생성
* GitHub에 SSH 공개키 등록
* Mac과 GitHub SSH 인증 확인
* GitHub `mint64-os-study` Public Repository 생성

### 이해한 것

* Git과 GitHub는 서로 다른 역할을 한다.
* SSH 공개키와 개인키의 역할이 다르다.
* Git commit에는 작성자 이름과 이메일 정보가 기록된다.

### 다음 시작점

* Mac에 로컬 프로젝트 폴더 생성
* Git 저장소 초기화
* GitHub 저장소와 연결


## 2026-09-02

### 오늘 한 것

* `~/Projects/mint64-os-study` 프로젝트 폴더 생성
* `git init`으로 로컬 Git 저장소 초기화
* 저장소 전용 GitHub noreply 이메일 설정
* `README.md`, `DEVLOG.md` 생성
* 프로젝트 README 작성

### 이해한 것

* `git init`을 실행하면 `.git` 디렉터리가 생성되고 해당 폴더가 Git 저장소가 된다.
* 새로 만든 파일은 처음에는 `Untracked` 상태이다.

### 다음 시작점

* `README.md`와 `DEVLOG.md`를 Git에 추가
* 첫 commit 생성
* GitHub 원격 저장소 연결
* 첫 push


## 2026-09-03

### 오늘 한 것

* 로컬 `mint64-os-study` 저장소와 GitHub 원격 저장소 연결 확인
* `git add`, `git commit`, `git push` 과정을 직접 수행
* GitHub에서 `README.md`, `DEVLOG.md`, `GIT_GUIDE.md`가 정상적으로 올라간 것을 확인
* Git/GitHub 사용법과 주요 명령어를 `GIT_GUIDE.md`에 정리
* Git의 기본 작업 흐름을 실제로 연습

### 이해한 것

* `git add`는 다음 commit에 포함할 변경사항을 Staging Area에 올리는 과정이다.
* `git commit`은 Staging된 변경사항을 로컬 Git History에 기록하는 과정이다.
* `git push`는 로컬에서 만든 commit을 GitHub 원격 저장소로 전송하는 과정이다.
* `git diff`는 아직 Staging하지 않은 변경사항을 확인하고, `git diff --staged`는 다음 commit에 들어갈 변경사항을 확인할 때 사용한다.
* Git과 GitHub는 같은 것이 아니며, Git은 로컬 변경 이력 관리 도구이고 GitHub는 Git 저장소를 원격에서 보관·공유하는 서비스이다.
* Commit message는 정해진 답을 쓰는 것이 아니라 실제 변경사항을 의미 있게 요약해서 작성해야 한다.
* GitHub 기록의 목적은 commit 수를 늘리는 것이 아니라 학습·구현·실패·디버깅 과정을 추적 가능하게 만드는 것이다.

### 막힌 것

* 없음

### 다음 시작점

* Stage 0의 다음 단계인 운영체제 개발환경 점검부터 시작
* Mac에 필요한 개발도구가 현재 어느 정도 설치되어 있는지 확인
* QEMU, NASM, Make, x86/x86-64 Cross Compiler 환경 구성 방향 확인


## 2026-09-04

### 오늘 한 것

* 『64비트 멀티코어 OS 원리와 구조』 2장 개발환경 구축을 시작하기 전에 현재 M4 Mac에서 사용할 개발환경 방향을 검토함.
* Windows 가상환경 + Cygwin 방식과 macOS에서 직접 Cross Compiler, NASM, Make, QEMU를 사용하는 방식을 비교함.
* 책을 기반으로 Mac에서 공부한 비공식 GitHub 자료도 참고하여 macOS 환경에서의 개발 가능성을 확인함.
* 별도 Windows 노트북 구매와 Parallels 사용 여부도 검토했으나 아직 확정하지 않음.

### 이해한 것

* Host가 Apple Silicon ARM64 Mac이어도 Cross Compiler와 QEMU를 이용하면 Target은 책과 동일한 x86/x86-64 OS로 유지할 수 있음.
* Cygwin은 Windows에서 Unix/Linux 계열 개발환경을 제공하기 위한 도구이며, macOS는 원래 Unix 계열 환경이므로 반드시 필요한 것은 아님.
* Mac에서 직접 개발할 경우 OS 코드 자체보다 Cross Compiler, NASM, Make, QEMU 등 개발도구의 호환성을 먼저 검증해야 함.
* 비공식 학습자의 Mac 개발 기록은 참고자료로 사용할 수 있지만, 책 저자의 공식 개발환경과 동일한 기준으로 보아서는 안 됨.

### 막힌 것

* M4 Mac에서 책의 전체 개발 흐름을 얼마나 수정 없이 따라갈 수 있을지는 아직 실제로 검증하지 않음.
* Windows/Cygwin 방식과 macOS 네이티브 방식 중 최종 개발환경을 아직 확정하지 않음.

### 다음 시작점

* 『64비트 멀티코어 OS 원리와 구조』 2장을 기준으로 M4 Mac에서 필요한 개발도구를 하나씩 확인하고 실제 개발환경 구축을 시작하기.
* 우선 NASM, Make, QEMU, x86/x86-64 Cross Compiler의 현재 설치 여부와 사용 가능 여부부터 확인하기.


## 2026-09-05

### 오늘 한 것

* 『64비트 멀티코어 OS 원리와 구조』 2장 개발환경 구축을 시작함.
* 책의 2.1 Cygwin/GCC 환경이 M4 Mac에서는 어떻게 대응되는지 확인함.
* 현재 Mac의 `gcc`가 GNU GCC가 아니라 Apple Clang 17이며, Host 아키텍처가 `arm64`임을 확인함.
* Homebrew에서 x86용 크로스 컴파일러 패키지를 조사함.
* `x86_64-elf-gcc`와 `x86_64-elf-binutils`를 설치함.
* `x86_64-elf-gcc --version`을 통해 GCC 16.2.0이 정상 설치된 것을 확인함.
* `x86_64-elf-gcc -print-multi-lib` 결과가 `.;`로 나타나는 것을 확인함.

### 이해한 것

* M4 Mac의 Host 아키텍처는 ARM64이지만, Cross Compiler를 사용하면 x86/x86-64용 코드를 생성할 수 있음.
* macOS의 `gcc` 명령은 실제 GNU GCC가 아니라 Apple Clang을 가리킬 수 있음.
* 책에서 사용하는 크로스 컴파일러의 Target은 `x86_64-pc-linux`이고, 현재 설치한 Homebrew 툴체인의 Target은 `x86_64-elf`이므로 두 환경이 완전히 동일하지는 않음.
* 개발환경 구축 자체가 프로젝트의 목적은 아니므로, 실제 OS 학습보다 환경 문제에 지나치게 많은 시간을 사용하지 않는 것이 필요함.

### 막힌 것

* 현재 설치한 `x86_64-elf-gcc`가 책에서 필요한 32비트/64비트 빌드를 모두 문제없이 처리할 수 있는지는 아직 확인하지 않음.
* M4 Mac에서 계속 직접 개발할지, Ubuntu ARM VM이나 별도의 x86 환경을 사용할지 아직 최종 결정하지 않음.
* 개발환경 문제에 예상보다 시간이 많이 소요되어 오늘은 추가 검증을 진행하지 않음.

### 다음 시작점

* 멘토님께 M4 Mac에서 MINT64 OS 개발환경을 어떤 방식으로 구성하는 것이 가장 안정적인지 조언을 구하기.
* 멘토님의 의견을 바탕으로 개발환경 방향을 확정한 뒤 2장 개발환경 구축을 이어가기.


## 2026-09-06

### 오늘 한 것

- 멘토님께 M4 Mac에서의 운영체제 개발환경 구성 방향에 대해 조언을 구함.
- Ubuntu 기반 개발환경과 x86/x64 크로스 컴파일 방향을 우선 검토하기로 함.
- 구현에 들어가기 전에 책의 전체 흐름을 먼저 파악하는 방식으로 학습 계획을 조정함.
- 『64비트 멀티코어 OS 원리와 구조』 2장을 읽기 시작했고, 2.1 GCC 설치 부분까지 학습함.

### 이해한 것

- Cygwin은 Windows에서 Unix/Linux 계열 개발환경을 제공하기 위한 도구임.
- GCC는 C 코드를 컴파일하는 도구이며, 이후 x86/x64용 OS 코드를 만들기 위해서는 Cross Compiler가 필요함.
- M4 Mac을 사용하더라도 개발 대상 OS의 아키텍처는 ARM이 아니라 책과 동일한 x86/x64로 유지해야 함.
- 개발환경 구축 이후에는 코드 작성과 OS 구조 이해가 핵심이므로, 초기 환경설정에 지나치게 매몰되지 않는 것이 중요함.

### 막힌 것

- M4 Mac에서 사용할 최종 개발환경은 아직 완전히 구축하지 않음.
- Ubuntu ARM VM과 x86/x64 Cross Compiler 환경을 실제로 어떻게 구성할지는 이후 확인이 필요함.

### 다음 시작점

- 2.2 크로스 컴파일러 만들기부터 읽기.
- GNU Binutils와 GCC Cross Compiler가 각각 어떤 역할을 하는지 먼저 이해하기.


## 2026-09-07

### 오늘 한 것

- 없음

### 다음 시작점

- 2.2 크로스 컴파일러 만들기부터 다시 시작.


## 2026-09-08

### 오늘 한 것

- 없음

### 다음 시작점

- 2.2 크로스 컴파일러 만들기부터 다시 시작.


## 2026-09-09

### 오늘 한 것

- 『64비트 멀티코어 OS 원리와 구조』 2장을 이어서 학습함.
- 크로스 컴파일러와 GNU Binutils의 역할을 정리함.
- GCC의 부트스트랩 과정을 학습함.
- NASM, Eclipse, QEMU의 역할을 정리함.
- x86, x86_64, ARM, PowerPC 아키텍처의 차이를 비교함.

### 이해한 것

#### 크로스 컴파일러

크로스 컴파일러는 자신이 실행되는 플랫폼이 아닌 다른 플랫폼에서 동작할 실행 파일을 생성하는 컴파일러이다.

『64비트 멀티코어 OS 원리와 구조』에서는 64비트 운영체제를 만들지만, 개발 과정에서 32비트 코드와 64비트 코드가 모두 필요하다. 따라서 다양한 타깃에 맞는 코드를 생성할 수 있는 크로스 컴파일 환경이 필요하다.

#### GNU Binutils

GNU Binutils는 바이너리 파일을 다루는 여러 유틸리티의 집합이다.

컴파일 과정에서 생성된 오브젝트 파일을 링크하여 실행 파일이나 라이브러리 파일을 생성하는 데 사용된다. 이후 OS 이미지를 만들기 위해 필요한 여러 바이너리 처리 작업에도 사용된다.

#### GCC의 부트스트랩

GCC는 자신을 컴파일할 때 여러 단계를 거쳐 컴파일러를 생성한다.

첫 번째 단계에서는 기존 GCC를 이용해 새로운 GCC를 만들고, 이후 단계에서는 앞 단계에서 생성한 GCC를 이용해 다시 GCC를 컴파일한다. 이러한 과정을 부트스트랩이라고 한다.

부트스트랩 과정을 거치는 이유는 새로 생성한 컴파일러가 정상적으로 동작하는지 확인하고, 보다 안정적인 컴파일러를 생성하기 위해서이다.

#### NASM

NASM은 The Netwide Assembler의 약자로, Windows와 Linux 등 다양한 플랫폼을 지원하는 무료 어셈블러이다.

어셈블리어로 작성한 코드를 CPU가 실행할 수 있는 기계어 형태로 변환하는 역할을 한다.

#### Eclipse

Eclipse는 Windows와 Linux 등 다양한 플랫폼에서 사용할 수 있는 무료 통합 개발 환경(IDE)이다.

여러 프로그래밍 언어를 지원하며, 프로젝트 옵션 설정이나 단축키 기능 등을 제공해 코드 작성과 프로젝트 관리를 편리하게 해준다.

#### QEMU

QEMU는 오픈소스 프로세서 에뮬레이터이다.

x86, x86_64, ARM, PowerPC 등 다양한 프로세서 아키텍처를 소프트웨어적으로 구현할 수 있으며, 멀티코어 환경도 에뮬레이션할 수 있다.

운영체제 개발에서는 실제 하드웨어에 바로 설치하지 않고, QEMU 내부에서 만든 OS를 실행하고 테스트할 수 있다.

#### x86, x86_64, ARM, PowerPC

- **x86**  
  Intel 8086 계열에서 시작한 아키텍처로, 일반적으로 32비트 x86 환경을 의미한다.

- **x86_64**  
  x86 아키텍처를 64비트로 확장한 구조이다. 현재 Intel과 AMD 기반 PC에서 널리 사용된다.

- **ARM**  
  Apple Silicon, 스마트폰, 일부 서버 등에 사용되는 아키텍처이다. x86과는 레지스터 구조, 명령어, 부팅 방식, 예외 처리, 메모리 관리, CPU 모드 등이 다르다.

- **PowerPC**  
  과거 Mac과 IBM 시스템 등에서 많이 사용된 CPU 아키텍처이다. 현재 일반 PC에서는 거의 사용되지 않는다.

### 막힌 것

- 개발환경을 실제로 어떤 방식으로 최종 구성할지는 아직 확정하지 않음.
- 현재는 책의 개발도구와 아키텍처 개념 이해를 우선하고 있으며, 실제 환경 구축은 이후 진행할 예정임.

### 다음 시작점

- 2장의 전체 흐름을 한 번 복습하기.
- 개발환경을 구성하는 각 도구가 실제 빌드 과정에서 어떻게 연결되는지 정리하기.
- 이후 3장으로 넘어가 x86/x86_64 프로세서 구조와 운영 모드 학습 시작하기.


## 2026-09-10

### 오늘 한 것

- 『64비트 멀티코어 OS 원리와 구조』 3.1 운영 모드를 학습함.
- x86_64 프로세서의 리얼 모드, 보호 모드, IA-32e 모드, 시스템 관리 모드, 가상 8086 모드를 정리함.
- 리얼 모드 → 보호 모드 → IA-32e 모드로 이어지는 운영 모드 전환 흐름을 확인함.
- 리얼 모드에서 BIOS 기능을 사용할 수 있는 이유와 디바이스 드라이버의 기본 개념을 함께 정리함.

### 이해한 것

#### 리얼 모드

리얼 모드는 x86 계열 프로세서가 전원이 켜지거나 리셋된 뒤 처음 진입하는 기본 모드이다.

16비트 모드로 동작하며 8086 프로세서와 호환되고, 최대 1MB의 주소 공간을 지원한다.

리얼 모드에서는 BIOS 기능을 사용할 수 있기 때문에 초기 부팅 단계에서는 직접 디바이스 드라이버를 만들지 않고도 기본적인 입출력 기능을 이용할 수 있다.

초기 부팅 과정에서는 대부분의 작업을 어셈블리어로 처리한다.

#### 보호 모드

보호 모드는 32비트 모드로 동작하며, 리얼 모드보다 확장된 기능을 제공한다.

대표적으로 다음과 같은 기능을 지원한다.

- 메모리 보호
- 세그먼테이션
- 페이징
- 멀티태스킹

최대 4GB의 주소 공간을 지원하며, 리얼 모드에서 IA-32e 모드로 전환하기 위해서는 보호 모드를 거쳐야 한다.

#### IA-32e 모드

IA-32e 모드는 64비트 환경을 지원하는 운영 모드이다.

두 개의 서브모드로 구성된다.

- Compatibility Mode
  - 기존 32비트 코드 실행을 지원함.
- 64-bit Mode
  - 64비트 레지스터와 확장된 주소 공간을 사용할 수 있음.

이 책에서 최종적으로 구현하는 64비트 OS는 IA-32e 모드의 64비트 모드에서 동작한다.

#### 시스템 관리 모드

시스템 관리 모드는 전원 관리나 하드웨어 제어와 같은 시스템 수준의 특수 기능을 처리하기 위한 모드이다.

일반적인 OS 코드가 주로 사용하는 운영 모드는 아니다.

#### 가상 8086 모드

가상 8086 모드는 보호 모드 내부에서 8086과 유사한 실행 환경을 제공하는 모드이다.

보호 모드의 제어 아래에서 리얼 모드용 16비트 코드를 실행할 수 있도록 하는 방식이다.

#### 디바이스 드라이버

디바이스 드라이버는 운영체제와 하드웨어 장치 사이에서 신호를 전달하고 장치를 제어하는 시스템 소프트웨어이다.

장치를 초기화하고 관리하며, 운영체제가 복잡한 하드웨어 구조를 직접 모두 다루지 않고 일정한 방식으로 장치를 사용할 수 있도록 중간 역할을 한다.

리얼 모드에서는 BIOS 기능을 사용할 수 있기 때문에 초기 부팅 단계에서는 직접 디바이스 드라이버를 만들지 않고도 일부 기본적인 입출력 기능을 사용할 수 있다.

#### 운영 모드 전환 흐름

x86_64 기반 64비트 OS는 일반적으로 다음과 같은 순서로 운영 모드를 전환한다.

Real Mode
→ Protected Mode
→ IA-32e Mode
→ 64-bit Mode

운영 모드 전환은 단순히 모드 이름만 바꾸는 것이 아니라, CR0, CR4, EFER 등의 제어 레지스터에서 필요한 비트를 설정하는 과정을 통해 이루어진다.

특히 리얼 모드에서 보호 모드로 전환할 때는 CR0 레지스터의 PE 비트를 설정하고, 보호 모드에서 IA-32e 모드로 전환할 때는 페이징 및 PAE, LME와 관련된 설정이 필요하다.

### 다음 시작점

- 3.1의 운영 모드 전환 다이어그램을 다시 보면서 각 모드 사이의 전환 조건을 조금 더 자세히 학습하기.
- 특히 리얼 모드 → 보호 모드 → IA-32e 모드 전환 과정에서 CR0, CR4, EFER 레지스터의 어떤 비트가 사용되는지 흐름 중심으로 확인하기.
- 각 전환이 왜 필요한지와 전환 전후 CPU 상태가 어떻게 달라지는지도 함께 정리하기.
- 이후 3.2 운영 모드와 레지스터로 넘어가기.
